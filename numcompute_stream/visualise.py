class StreamVisualiser:

    def plot_metric_over_time(metric_values, title, ylabel):
        ...

    def compare_models(metric1, metric2, labels):
        ...

    def plot_predictions_vs_ground_truth(y_true, y_pred):
        ...